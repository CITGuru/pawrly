# A multi-source fragment owned by, say, a product team. Committed and
# reviewed independently of the root config.
sources:
  - name: oltp
    kind: postgres
    config:
      dsn: ${secret:PG_DSN}

  - name: billing
    kind: http
    config:
      base_url: https://api.stripe.com
      token: ${secret:STRIPE_API_KEY}
