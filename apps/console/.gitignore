node_modules
# Generated: `pnpm build` writes dist/ (embedded by the `console` feature) and
# `buf generate` writes src/gen/. Both are produced in CI before they're used.
dist
src/gen
# Generated before `pnpm dev` / `pnpm build` by scripts/build-catalog.mjs.
src/catalog/generated.ts
*.local
.DS_Store
